#include <bits/stdc++.h>

using namespace std;

int main() {
	int t;
	cin>>t;
	while(t>0){
	    int n;
	    cin>>n;
	    int arr[n];
	    for(int i=0;i<n;i++){
	        cin>>arr[i];
	    }
	    int ans=0;
	     for(int i=0;i<n;i++){

	             ans= __gcd(ans,arr[i]);

	         }
	         for(int i=0;i<n;i++){
	             cout<<arr[i]/ans<<" ";
	         }cout<<endl;

   t--;
	}
	return 0;
}
